# =============================================================
# requirements.txt
# =============================================================
# Install everything with:
#   pip install -r requirements.txt

# Database — synchronous (Mímir, Lex) and async (Ares)
psycopg2-binary==2.9.9          # PostgreSQL driver (sync — used by Mímir/Lex)
asyncpg==0.29.0                 # PostgreSQL driver (async — used by Ares agents)

# HTTP — synchronous (Mímir, Lex) and async (Ares)
requests==2.32.3                # HTTP client (sync — used by Mímir/Lex)
aiohttp==3.9.5                  # HTTP client (async — used by Ares agents)

# Claude API
anthropic==0.40.0               # Anthropic Python SDK

# Scheduler
APScheduler==3.10.4             # Job scheduler

# Environment variables
python-dotenv==1.0.1

# Telegram bot (for sending alerts and digests)
python-telegram-bot==21.6

# Utilities
python-dateutil==2.9.0          # Robust date parsing
feedparser                       # RSS/Atom feed parsing (Lex)
